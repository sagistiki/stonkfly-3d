"""Read-only 3D viewer for a Stonkfly run directory.

Lives outside the ``stonkfly`` package on purpose: run provenance hashes every
source file in that package, so viewer changes must not invalidate a run.
"""
